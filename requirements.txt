# PatrolIQ — Streamlit Cloud requirements
# Only packages actually imported by streamlit_app.py and pages/
# No version pins — lets Streamlit Cloud install latest compatible wheels
# (strict pins are in requirements-dev.txt for local/Docker use)

streamlit
pandas
numpy
plotly
folium
streamlit-folium
scikit-learn
joblib
matplotlib
